'use client'

import { useEffect } from 'react'

const PageClient: React.FC = () => {
  useEffect(() => {
    // Можно добавить client-side логику если потребуется
  }, [])

  return null
}

export default PageClient
